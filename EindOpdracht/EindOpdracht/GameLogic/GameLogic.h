typedef enum {
    CORRECTPERC_INVALID,
    BUFFERSIZE_INVALID,
    CORRECTINPUTVALUE_INVALID,
} GAMELOGIC_ERROR;

typedef enum {
    UP,
    DOWN,
    LEFT,
    RIGHT,
    FORWARD,
    BACKWARD,
} DIRECTION;

typedef DIRECTION dirs[] = {UP, DOWN, LEFT, RIGHT, FORWARD, BACKWARD};